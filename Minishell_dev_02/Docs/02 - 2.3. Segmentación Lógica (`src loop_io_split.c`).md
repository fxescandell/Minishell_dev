**Fase 2.3: Segmentación Lógica**. 🧠✂️

Aquí es donde tu shell empieza a demostrar que es más listo que un simple `split`.

**El Problema:**
Si usas `ft_split(line, ';')` (de tu libft) para separar comandos, funcionará con `ls; pwd`.
PERO fallará estrepitosamente con esto:
`echo "hola; mundo"`

Un `split` normal rompería la cadena en medio de las comillas, y el comando `echo` recibiría solo `"hola`, lo cual es un error de sintaxis.

**La Solución (`src/loop_io_split.c`):**
Necesitamos una función que recorra la línea carácter a carácter y sepa cuándo está "dentro" de comillas para ignorar los puntos y coma que encuentre allí.